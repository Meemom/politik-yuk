# Workers

Background ingestion and enrichment workers will live here.

Milestone 1 only reserves the package boundary. Later milestones will add article discovery, parsing, deduplication, embeddings, claim extraction, entity extraction, indexing, retries, and job status tracking.
